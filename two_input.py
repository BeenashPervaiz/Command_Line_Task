#take 2 input from user in one line
#when we want to get output we should enter the space  or comma after print the message. if we want comma between the user input then we should write comma inside the split(",") function
name, age = input("Enter your name and age").split()
print(name)
print(age)
